# Keep attributes file itself in LF
.gitattributes text eol=lf

# Always use LF in shell scripts
*.sh text eol=lf

# Keep Python files LF so Black output is stable across OSes
*.py text eol=lf

# Explicitly enforce LF for CI-sensitive Django entrypoint
backend/manage.py text eol=lf